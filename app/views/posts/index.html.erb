<h1>Blog</h1>

<p>Our blog has <%= Post.published.count %> posts and counting!</p>

<ul>
  <% @posts.each do |post| %>
    <li>
      <% if post.draft? %>
        <span class="bg-yellow-100 text-yellow-800 text-xs font-medium me-2 px-2.5 py-0.5 rounded dark:bg-yellow-900 dark:text-yellow-300">Draft</span>
      <% elsif post.scheduled? %>
        <span class="bg-blue-100 text-blue-800 text-xs font-medium me-2 px-2.5 py-0.5 rounded dark:bg-blue-900 dark:text-blue-300">Scheduled</span>
      <% end %>
      <%= link_to post.title, post %>
    </li>
  <% end %>
</ul>

<%= link_to "New Post", new_post_path %>